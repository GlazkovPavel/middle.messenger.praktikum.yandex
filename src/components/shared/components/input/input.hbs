
  <input
    name="{{ name }}"
    id="{{id}}"
    type="{{ type }}"
    data-name="{{ dataName }}"
    placeholder="{{ placeholder }}"
    minlength="{{ minlength }}"
    class="{{ class }}"
    value="{{ value }}"
    {{ required }}
  />


