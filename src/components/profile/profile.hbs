 <div class="container__profile">
   {{{buttonBack}}}
  <div class="profile-main">
    {{{avatar}}}
    {{{ popup }}}
    <div class="profile-form">
      {{#if isProfileEdit}}
        {{{ profileEdit }}}
      {{else}}
        {{{ profileEditPassword }}}
      {{/if}}
    </div>
    <div class="button__container">
        {{# if isCanEdit }}
          {{{ buttonSave }}}
        {{else}}
          {{{ buttonEdit }}}
          {{{ buttonEditPassword }}}
        {{/if}}
      {{{ buttonSignOut }}}
    </div>

  </div>

</div>
